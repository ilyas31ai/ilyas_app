{
  "name": "ilyas-app-firestore-tests",
  "version": "1.0.0",
  "description": "Tests de sécurité Firestore pour ScolarAI Educative",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch --runInBand"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase-admin": "^11.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 30000
  }
}
